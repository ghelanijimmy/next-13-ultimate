'use client';

type AddToCartProps = {

};
export const AddToCart = (props: AddToCartProps) => {
    return (
        <div>
            <button onClick={() => {}}>Add to Cart</button>
        </div>
    );
};